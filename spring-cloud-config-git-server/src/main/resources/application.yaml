server:  
  port: ${PORT:8888}
  
spring:
  profiles:
     active: native
  application:
    name: config-server
#  cloud:
#      config:
#         server:
#            native:
#               search-locations: classpath:/config
  security:
    user:
      name: myusername
      password: mypassword