💡 Prefer Objects.equals(a, b) for null-safe equality checks.

✅ a.equals(b) NPEs when a is null

✅ Objects.equals handles null on either side

✅ Both null → true; one null → false; else a.equals(b)

#Java #Objects
