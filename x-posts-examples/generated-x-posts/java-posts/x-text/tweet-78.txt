💡 Java 8+ CompletableFuture: chain async steps without blocking the thread on every I/O call.

✅ Chain: supplyAsync → thenApply → thenCompose → thenAccept (fetch user, then orders)

#Java #Concurrency
