💡 Java: Use Optional.ifPresentOrElse() instead of if (x != null).

✅ One call handles present and empty cases

✅ Avoids isPresent() + get() boilerplate

✅ Good when both branches run side effects

#Java #Optional
