💡 Use Files.isSameFile(a, b) to check if two paths are the same file.

✅ Path.equals only compares path text

✅ isSameFile checks the real file (follows symlinks)

✅ Catches ./file vs file and link vs target

#Java #Files
