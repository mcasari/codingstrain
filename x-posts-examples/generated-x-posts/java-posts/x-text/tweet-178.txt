💡 Use Optional.orElseThrow() when a missing value is a bug.

✅ orElse(null) → NPE later, far from the lookup

✅ orElseThrow() stops at the source with a clear exception

✅ Pass a supplier so the message says what was missing

#Java #Optional
