💡 Spring Boot 4: Config keys move — use properties-migrator as a temporary guide.

✅ Add it → startup logs tell you which keys renamed

✅ Old keys still work while you fix application.yml

✅ Update the keys, then delete the dependency

#SpringBoot4 #Migration
